# Runtime dependencies of the SURF measurement terminal.
# Validated against Python 3.13.0 with the versions in requirements.lock.txt.
# Upper bounds are deliberate: terminal.py uses the short-form Qt enum access
# (QDialogButtonBox.Ok, QMessageBox.Yes) that a PySide6 major bump would break.
pyserial>=3.5,<4
matplotlib>=3.7,<4
PySide6>=6.5,<7

# tkinter is used by software/surf_terminal/tools/backlash_calculator.py.
# It ships with CPython but is a separate OS package on many Linux distributions
# (e.g. `apt install python3-tk`). It cannot be installed from PyPI.
